//! This code was AUTOGENERATED using the codama library.
//! Please DO NOT EDIT THIS FILE, instead use visitors
//! to add features, then rerun codama to update it.
//!
//! <https://github.com/codama-idl/codama>
//!

use borsh::BorshDeserialize;
use borsh::BorshSerialize;

pub const REANCHOR_DISCRIMINATOR: u8 = 40;

/// Accounts.
#[derive(Debug)]
pub struct Reanchor {
    pub market_authority: solana_pubkey::Pubkey,

    pub config: solana_pubkey::Pubkey,

    pub market: solana_pubkey::Pubkey,
}

impl Reanchor {
    pub fn instruction(&self, args: ReanchorInstructionArgs) -> solana_instruction::Instruction {
        self.instruction_with_remaining_accounts(args, &[])
    }
    #[allow(clippy::arithmetic_side_effects)]
    #[allow(clippy::vec_init_then_push)]
    pub fn instruction_with_remaining_accounts(
        &self,
        args: ReanchorInstructionArgs,
        remaining_accounts: &[solana_instruction::AccountMeta],
    ) -> solana_instruction::Instruction {
        let mut accounts = Vec::with_capacity(3 + remaining_accounts.len());
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.market_authority,
            true,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.config,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new(self.market, false));
        accounts.extend_from_slice(remaining_accounts);
        let mut data = ReanchorInstructionData::new().try_to_vec().unwrap();
        let mut args = args.try_to_vec().unwrap();
        data.append(&mut args);

        solana_instruction::Instruction {
            program_id: crate::CLOB_ID,
            accounts,
            data,
        }
    }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct ReanchorInstructionData {
    discriminator: u8,
}

impl ReanchorInstructionData {
    pub fn new() -> Self {
        Self { discriminator: 40 }
    }

    pub(crate) fn try_to_vec(&self) -> Result<Vec<u8>, std::io::Error> {
        borsh::to_vec(self)
    }
}

impl Default for ReanchorInstructionData {
    fn default() -> Self {
        Self::new()
    }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct ReanchorInstructionArgs {
    pub target_anchor: u32,
    pub max_levels: u32,
}

impl ReanchorInstructionArgs {
    pub(crate) fn try_to_vec(&self) -> Result<Vec<u8>, std::io::Error> {
        borsh::to_vec(self)
    }
}

/// Instruction builder for `Reanchor`.
///
/// ### Accounts:
///
///   0. `[signer]` market_authority
///   1. `[]` config
///   2. `[writable]` market
#[derive(Clone, Debug, Default)]
pub struct ReanchorBuilder {
    market_authority: Option<solana_pubkey::Pubkey>,
    config: Option<solana_pubkey::Pubkey>,
    market: Option<solana_pubkey::Pubkey>,
    target_anchor: Option<u32>,
    max_levels: Option<u32>,
    __remaining_accounts: Vec<solana_instruction::AccountMeta>,
}

impl ReanchorBuilder {
    pub fn new() -> Self {
        Self::default()
    }
    #[inline(always)]
    pub fn market_authority(&mut self, market_authority: solana_pubkey::Pubkey) -> &mut Self {
        self.market_authority = Some(market_authority);
        self
    }
    #[inline(always)]
    pub fn config(&mut self, config: solana_pubkey::Pubkey) -> &mut Self {
        self.config = Some(config);
        self
    }
    #[inline(always)]
    pub fn market(&mut self, market: solana_pubkey::Pubkey) -> &mut Self {
        self.market = Some(market);
        self
    }
    #[inline(always)]
    pub fn target_anchor(&mut self, target_anchor: u32) -> &mut Self {
        self.target_anchor = Some(target_anchor);
        self
    }
    #[inline(always)]
    pub fn max_levels(&mut self, max_levels: u32) -> &mut Self {
        self.max_levels = Some(max_levels);
        self
    }
    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(&mut self, account: solana_instruction::AccountMeta) -> &mut Self {
        self.__remaining_accounts.push(account);
        self
    }
    /// Add additional accounts to the instruction.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[solana_instruction::AccountMeta],
    ) -> &mut Self {
        self.__remaining_accounts.extend_from_slice(accounts);
        self
    }
    #[allow(clippy::clone_on_copy)]
    pub fn instruction(&self) -> solana_instruction::Instruction {
        let accounts = Reanchor {
            market_authority: self.market_authority.expect("market_authority is not set"),
            config: self.config.expect("config is not set"),
            market: self.market.expect("market is not set"),
        };
        let args = ReanchorInstructionArgs {
            target_anchor: self
                .target_anchor
                .clone()
                .expect("target_anchor is not set"),
            max_levels: self.max_levels.clone().expect("max_levels is not set"),
        };

        accounts.instruction_with_remaining_accounts(args, &self.__remaining_accounts)
    }
}

/// `reanchor` CPI accounts.
pub struct ReanchorCpiAccounts<'a, 'b> {
    pub market_authority: &'b solana_account_info::AccountInfo<'a>,

    pub config: &'b solana_account_info::AccountInfo<'a>,

    pub market: &'b solana_account_info::AccountInfo<'a>,
}

/// `reanchor` CPI instruction.
pub struct ReanchorCpi<'a, 'b> {
    /// The program to invoke.
    pub __program: &'b solana_account_info::AccountInfo<'a>,

    pub market_authority: &'b solana_account_info::AccountInfo<'a>,

    pub config: &'b solana_account_info::AccountInfo<'a>,

    pub market: &'b solana_account_info::AccountInfo<'a>,
    /// The arguments for the instruction.
    pub __args: ReanchorInstructionArgs,
}

impl<'a, 'b> ReanchorCpi<'a, 'b> {
    pub fn new(
        program: &'b solana_account_info::AccountInfo<'a>,
        accounts: ReanchorCpiAccounts<'a, 'b>,
        args: ReanchorInstructionArgs,
    ) -> Self {
        Self {
            __program: program,
            market_authority: accounts.market_authority,
            config: accounts.config,
            market: accounts.market,
            __args: args,
        }
    }
    #[inline(always)]
    pub fn invoke(&self) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], &[])
    }
    #[inline(always)]
    pub fn invoke_with_remaining_accounts(
        &self,
        remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], remaining_accounts)
    }
    #[inline(always)]
    pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(signers_seeds, &[])
    }
    #[allow(clippy::arithmetic_side_effects)]
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed_with_remaining_accounts(
        &self,
        signers_seeds: &[&[&[u8]]],
        remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> solana_program_error::ProgramResult {
        let mut accounts = Vec::with_capacity(3 + remaining_accounts.len());
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.market_authority.key,
            true,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.config.key,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new(
            *self.market.key,
            false,
        ));
        remaining_accounts.iter().for_each(|remaining_account| {
            accounts.push(solana_instruction::AccountMeta {
                pubkey: *remaining_account.0.key,
                is_signer: remaining_account.1,
                is_writable: remaining_account.2,
            })
        });
        let mut data = ReanchorInstructionData::new().try_to_vec().unwrap();
        let mut args = self.__args.try_to_vec().unwrap();
        data.append(&mut args);

        let instruction = solana_instruction::Instruction {
            program_id: crate::CLOB_ID,
            accounts,
            data,
        };
        let mut account_infos = Vec::with_capacity(4 + remaining_accounts.len());
        account_infos.push(self.__program.clone());
        account_infos.push(self.market_authority.clone());
        account_infos.push(self.config.clone());
        account_infos.push(self.market.clone());
        remaining_accounts
            .iter()
            .for_each(|remaining_account| account_infos.push(remaining_account.0.clone()));

        if signers_seeds.is_empty() {
            solana_cpi::invoke(&instruction, &account_infos)
        } else {
            solana_cpi::invoke_signed(&instruction, &account_infos, signers_seeds)
        }
    }
}

/// Instruction builder for `Reanchor` via CPI.
///
/// ### Accounts:
///
///   0. `[signer]` market_authority
///   1. `[]` config
///   2. `[writable]` market
#[derive(Clone, Debug)]
pub struct ReanchorCpiBuilder<'a, 'b> {
    instruction: Box<ReanchorCpiBuilderInstruction<'a, 'b>>,
}

impl<'a, 'b> ReanchorCpiBuilder<'a, 'b> {
    pub fn new(program: &'b solana_account_info::AccountInfo<'a>) -> Self {
        let instruction = Box::new(ReanchorCpiBuilderInstruction {
            __program: program,
            market_authority: None,
            config: None,
            market: None,
            target_anchor: None,
            max_levels: None,
            __remaining_accounts: Vec::new(),
        });
        Self { instruction }
    }
    #[inline(always)]
    pub fn market_authority(
        &mut self,
        market_authority: &'b solana_account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.market_authority = Some(market_authority);
        self
    }
    #[inline(always)]
    pub fn config(&mut self, config: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.config = Some(config);
        self
    }
    #[inline(always)]
    pub fn market(&mut self, market: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.market = Some(market);
        self
    }
    #[inline(always)]
    pub fn target_anchor(&mut self, target_anchor: u32) -> &mut Self {
        self.instruction.target_anchor = Some(target_anchor);
        self
    }
    #[inline(always)]
    pub fn max_levels(&mut self, max_levels: u32) -> &mut Self {
        self.instruction.max_levels = Some(max_levels);
        self
    }
    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(
        &mut self,
        account: &'b solana_account_info::AccountInfo<'a>,
        is_writable: bool,
        is_signer: bool,
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .push((account, is_writable, is_signer));
        self
    }
    /// Add additional accounts to the instruction.
    ///
    /// Each account is represented by a tuple of the `AccountInfo`, a `bool` indicating whether the account is writable or not,
    /// and a `bool` indicating whether the account is a signer or not.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .extend_from_slice(accounts);
        self
    }
    #[inline(always)]
    pub fn invoke(&self) -> solana_program_error::ProgramResult {
        self.invoke_signed(&[])
    }
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
        let args = ReanchorInstructionArgs {
            target_anchor: self
                .instruction
                .target_anchor
                .clone()
                .expect("target_anchor is not set"),
            max_levels: self
                .instruction
                .max_levels
                .clone()
                .expect("max_levels is not set"),
        };
        let instruction = ReanchorCpi {
            __program: self.instruction.__program,

            market_authority: self
                .instruction
                .market_authority
                .expect("market_authority is not set"),

            config: self.instruction.config.expect("config is not set"),

            market: self.instruction.market.expect("market is not set"),
            __args: args,
        };
        instruction.invoke_signed_with_remaining_accounts(
            signers_seeds,
            &self.instruction.__remaining_accounts,
        )
    }
}

#[derive(Clone, Debug)]
struct ReanchorCpiBuilderInstruction<'a, 'b> {
    __program: &'b solana_account_info::AccountInfo<'a>,
    market_authority: Option<&'b solana_account_info::AccountInfo<'a>>,
    config: Option<&'b solana_account_info::AccountInfo<'a>>,
    market: Option<&'b solana_account_info::AccountInfo<'a>>,
    target_anchor: Option<u32>,
    max_levels: Option<u32>,
    /// Additional instruction accounts `(AccountInfo, is_writable, is_signer)`.
    __remaining_accounts: Vec<(&'b solana_account_info::AccountInfo<'a>, bool, bool)>,
}
