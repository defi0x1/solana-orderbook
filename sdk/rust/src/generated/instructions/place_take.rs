//! This code was AUTOGENERATED using the codama library.
//! Please DO NOT EDIT THIS FILE, instead use visitors
//! to add features, then rerun codama to update it.
//!
//! <https://github.com/codama-idl/codama>
//!

use borsh::BorshDeserialize;
use borsh::BorshSerialize;

pub const PLACE_TAKE_DISCRIMINATOR: u8 = 30;

/// Accounts.
#[derive(Debug)]
pub struct PlaceTake {
    pub owner: solana_pubkey::Pubkey,

    pub market: solana_pubkey::Pubkey,
}

impl PlaceTake {
    pub fn instruction(&self, args: PlaceTakeInstructionArgs) -> solana_instruction::Instruction {
        self.instruction_with_remaining_accounts(args, &[])
    }
    #[allow(clippy::arithmetic_side_effects)]
    #[allow(clippy::vec_init_then_push)]
    pub fn instruction_with_remaining_accounts(
        &self,
        args: PlaceTakeInstructionArgs,
        remaining_accounts: &[solana_instruction::AccountMeta],
    ) -> solana_instruction::Instruction {
        let mut accounts = Vec::with_capacity(2 + remaining_accounts.len());
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.owner, true,
        ));
        accounts.push(solana_instruction::AccountMeta::new(self.market, false));
        accounts.extend_from_slice(remaining_accounts);
        let mut data = PlaceTakeInstructionData::new().try_to_vec().unwrap();
        let mut args = args.try_to_vec().unwrap();
        data.append(&mut args);

        solana_instruction::Instruction {
            program_id: crate::CLOB_ID,
            accounts,
            data,
        }
    }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct PlaceTakeInstructionData {
    discriminator: u8,
}

impl PlaceTakeInstructionData {
    pub fn new() -> Self {
        Self { discriminator: 30 }
    }

    pub(crate) fn try_to_vec(&self) -> Result<Vec<u8>, std::io::Error> {
        borsh::to_vec(self)
    }
}

impl Default for PlaceTakeInstructionData {
    fn default() -> Self {
        Self::new()
    }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct PlaceTakeInstructionArgs {
    pub seat_index: u16,
    pub side: u8,
    pub limit_tick: u32,
    pub max_lots: u32,
    pub min_out: u64,
    pub flags: u8,
}

impl PlaceTakeInstructionArgs {
    pub(crate) fn try_to_vec(&self) -> Result<Vec<u8>, std::io::Error> {
        borsh::to_vec(self)
    }
}

/// Instruction builder for `PlaceTake`.
///
/// ### Accounts:
///
///   0. `[signer]` owner
///   1. `[writable]` market
#[derive(Clone, Debug, Default)]
pub struct PlaceTakeBuilder {
    owner: Option<solana_pubkey::Pubkey>,
    market: Option<solana_pubkey::Pubkey>,
    seat_index: Option<u16>,
    side: Option<u8>,
    limit_tick: Option<u32>,
    max_lots: Option<u32>,
    min_out: Option<u64>,
    flags: Option<u8>,
    __remaining_accounts: Vec<solana_instruction::AccountMeta>,
}

impl PlaceTakeBuilder {
    pub fn new() -> Self {
        Self::default()
    }
    #[inline(always)]
    pub fn owner(&mut self, owner: solana_pubkey::Pubkey) -> &mut Self {
        self.owner = Some(owner);
        self
    }
    #[inline(always)]
    pub fn market(&mut self, market: solana_pubkey::Pubkey) -> &mut Self {
        self.market = Some(market);
        self
    }
    #[inline(always)]
    pub fn seat_index(&mut self, seat_index: u16) -> &mut Self {
        self.seat_index = Some(seat_index);
        self
    }
    #[inline(always)]
    pub fn side(&mut self, side: u8) -> &mut Self {
        self.side = Some(side);
        self
    }
    #[inline(always)]
    pub fn limit_tick(&mut self, limit_tick: u32) -> &mut Self {
        self.limit_tick = Some(limit_tick);
        self
    }
    #[inline(always)]
    pub fn max_lots(&mut self, max_lots: u32) -> &mut Self {
        self.max_lots = Some(max_lots);
        self
    }
    #[inline(always)]
    pub fn min_out(&mut self, min_out: u64) -> &mut Self {
        self.min_out = Some(min_out);
        self
    }
    #[inline(always)]
    pub fn flags(&mut self, flags: u8) -> &mut Self {
        self.flags = Some(flags);
        self
    }
    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(&mut self, account: solana_instruction::AccountMeta) -> &mut Self {
        self.__remaining_accounts.push(account);
        self
    }
    /// Add additional accounts to the instruction.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[solana_instruction::AccountMeta],
    ) -> &mut Self {
        self.__remaining_accounts.extend_from_slice(accounts);
        self
    }
    #[allow(clippy::clone_on_copy)]
    pub fn instruction(&self) -> solana_instruction::Instruction {
        let accounts = PlaceTake {
            owner: self.owner.expect("owner is not set"),
            market: self.market.expect("market is not set"),
        };
        let args = PlaceTakeInstructionArgs {
            seat_index: self.seat_index.clone().expect("seat_index is not set"),
            side: self.side.clone().expect("side is not set"),
            limit_tick: self.limit_tick.clone().expect("limit_tick is not set"),
            max_lots: self.max_lots.clone().expect("max_lots is not set"),
            min_out: self.min_out.clone().expect("min_out is not set"),
            flags: self.flags.clone().expect("flags is not set"),
        };

        accounts.instruction_with_remaining_accounts(args, &self.__remaining_accounts)
    }
}

/// `place_take` CPI accounts.
pub struct PlaceTakeCpiAccounts<'a, 'b> {
    pub owner: &'b solana_account_info::AccountInfo<'a>,

    pub market: &'b solana_account_info::AccountInfo<'a>,
}

/// `place_take` CPI instruction.
pub struct PlaceTakeCpi<'a, 'b> {
    /// The program to invoke.
    pub __program: &'b solana_account_info::AccountInfo<'a>,

    pub owner: &'b solana_account_info::AccountInfo<'a>,

    pub market: &'b solana_account_info::AccountInfo<'a>,
    /// The arguments for the instruction.
    pub __args: PlaceTakeInstructionArgs,
}

impl<'a, 'b> PlaceTakeCpi<'a, 'b> {
    pub fn new(
        program: &'b solana_account_info::AccountInfo<'a>,
        accounts: PlaceTakeCpiAccounts<'a, 'b>,
        args: PlaceTakeInstructionArgs,
    ) -> Self {
        Self {
            __program: program,
            owner: accounts.owner,
            market: accounts.market,
            __args: args,
        }
    }
    #[inline(always)]
    pub fn invoke(&self) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], &[])
    }
    #[inline(always)]
    pub fn invoke_with_remaining_accounts(
        &self,
        remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], remaining_accounts)
    }
    #[inline(always)]
    pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(signers_seeds, &[])
    }
    #[allow(clippy::arithmetic_side_effects)]
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed_with_remaining_accounts(
        &self,
        signers_seeds: &[&[&[u8]]],
        remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> solana_program_error::ProgramResult {
        let mut accounts = Vec::with_capacity(2 + remaining_accounts.len());
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.owner.key,
            true,
        ));
        accounts.push(solana_instruction::AccountMeta::new(
            *self.market.key,
            false,
        ));
        remaining_accounts.iter().for_each(|remaining_account| {
            accounts.push(solana_instruction::AccountMeta {
                pubkey: *remaining_account.0.key,
                is_signer: remaining_account.1,
                is_writable: remaining_account.2,
            })
        });
        let mut data = PlaceTakeInstructionData::new().try_to_vec().unwrap();
        let mut args = self.__args.try_to_vec().unwrap();
        data.append(&mut args);

        let instruction = solana_instruction::Instruction {
            program_id: crate::CLOB_ID,
            accounts,
            data,
        };
        let mut account_infos = Vec::with_capacity(3 + remaining_accounts.len());
        account_infos.push(self.__program.clone());
        account_infos.push(self.owner.clone());
        account_infos.push(self.market.clone());
        remaining_accounts
            .iter()
            .for_each(|remaining_account| account_infos.push(remaining_account.0.clone()));

        if signers_seeds.is_empty() {
            solana_cpi::invoke(&instruction, &account_infos)
        } else {
            solana_cpi::invoke_signed(&instruction, &account_infos, signers_seeds)
        }
    }
}

/// Instruction builder for `PlaceTake` via CPI.
///
/// ### Accounts:
///
///   0. `[signer]` owner
///   1. `[writable]` market
#[derive(Clone, Debug)]
pub struct PlaceTakeCpiBuilder<'a, 'b> {
    instruction: Box<PlaceTakeCpiBuilderInstruction<'a, 'b>>,
}

impl<'a, 'b> PlaceTakeCpiBuilder<'a, 'b> {
    pub fn new(program: &'b solana_account_info::AccountInfo<'a>) -> Self {
        let instruction = Box::new(PlaceTakeCpiBuilderInstruction {
            __program: program,
            owner: None,
            market: None,
            seat_index: None,
            side: None,
            limit_tick: None,
            max_lots: None,
            min_out: None,
            flags: None,
            __remaining_accounts: Vec::new(),
        });
        Self { instruction }
    }
    #[inline(always)]
    pub fn owner(&mut self, owner: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.owner = Some(owner);
        self
    }
    #[inline(always)]
    pub fn market(&mut self, market: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.market = Some(market);
        self
    }
    #[inline(always)]
    pub fn seat_index(&mut self, seat_index: u16) -> &mut Self {
        self.instruction.seat_index = Some(seat_index);
        self
    }
    #[inline(always)]
    pub fn side(&mut self, side: u8) -> &mut Self {
        self.instruction.side = Some(side);
        self
    }
    #[inline(always)]
    pub fn limit_tick(&mut self, limit_tick: u32) -> &mut Self {
        self.instruction.limit_tick = Some(limit_tick);
        self
    }
    #[inline(always)]
    pub fn max_lots(&mut self, max_lots: u32) -> &mut Self {
        self.instruction.max_lots = Some(max_lots);
        self
    }
    #[inline(always)]
    pub fn min_out(&mut self, min_out: u64) -> &mut Self {
        self.instruction.min_out = Some(min_out);
        self
    }
    #[inline(always)]
    pub fn flags(&mut self, flags: u8) -> &mut Self {
        self.instruction.flags = Some(flags);
        self
    }
    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(
        &mut self,
        account: &'b solana_account_info::AccountInfo<'a>,
        is_writable: bool,
        is_signer: bool,
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .push((account, is_writable, is_signer));
        self
    }
    /// Add additional accounts to the instruction.
    ///
    /// Each account is represented by a tuple of the `AccountInfo`, a `bool` indicating whether the account is writable or not,
    /// and a `bool` indicating whether the account is a signer or not.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .extend_from_slice(accounts);
        self
    }
    #[inline(always)]
    pub fn invoke(&self) -> solana_program_error::ProgramResult {
        self.invoke_signed(&[])
    }
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
        let args = PlaceTakeInstructionArgs {
            seat_index: self
                .instruction
                .seat_index
                .clone()
                .expect("seat_index is not set"),
            side: self.instruction.side.clone().expect("side is not set"),
            limit_tick: self
                .instruction
                .limit_tick
                .clone()
                .expect("limit_tick is not set"),
            max_lots: self
                .instruction
                .max_lots
                .clone()
                .expect("max_lots is not set"),
            min_out: self
                .instruction
                .min_out
                .clone()
                .expect("min_out is not set"),
            flags: self.instruction.flags.clone().expect("flags is not set"),
        };
        let instruction = PlaceTakeCpi {
            __program: self.instruction.__program,

            owner: self.instruction.owner.expect("owner is not set"),

            market: self.instruction.market.expect("market is not set"),
            __args: args,
        };
        instruction.invoke_signed_with_remaining_accounts(
            signers_seeds,
            &self.instruction.__remaining_accounts,
        )
    }
}

#[derive(Clone, Debug)]
struct PlaceTakeCpiBuilderInstruction<'a, 'b> {
    __program: &'b solana_account_info::AccountInfo<'a>,
    owner: Option<&'b solana_account_info::AccountInfo<'a>>,
    market: Option<&'b solana_account_info::AccountInfo<'a>>,
    seat_index: Option<u16>,
    side: Option<u8>,
    limit_tick: Option<u32>,
    max_lots: Option<u32>,
    min_out: Option<u64>,
    flags: Option<u8>,
    /// Additional instruction accounts `(AccountInfo, is_writable, is_signer)`.
    __remaining_accounts: Vec<(&'b solana_account_info::AccountInfo<'a>, bool, bool)>,
}
